apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dagster.fullname" . }}-test-connection"
  labels:
    {{- include "dagster.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dagster.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
